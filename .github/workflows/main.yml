name: Sync Past Submissions

on:
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: garimasingh128/codeforces-sync-action@v1.0.1
        with:
          max_submissions: 500
